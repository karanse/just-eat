  <div class="list-card">
    <div class="img" style="background-image: url(<%=restaurant.logo_url %>)"> </div>
    <div class="list-card-info">
      <div>
        <p> <%= restaurant.name.capitalize %> </p>
      </div>
      <h2 class="list-card-pricing"> <%= restaurant.rating.nil? ? "0" : restaurant.rating%></h2>
      <p><%= restaurant.city %></p>
      <p><%= restaurant.cuisines %></p>
    </div>
  </div>
